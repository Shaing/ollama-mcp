{
  "mcpServers": {
    "ollama-agent": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "ollama-agent"],
      "env": {
        "OLLAMA_AGENT_PROFILE": "${OLLAMA_AGENT_PROFILE:-trio}"
      },
      "timeout": 600000
    }
  }
}
